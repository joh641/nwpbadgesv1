%h1 Badges Display

- @badges.each do |badge|
  .col-md-3
    .badge-container
      .badge-info
        .image= link_to image_tag(badge.image.url(:medium)), badge_path(badge)
        .title= badge.name
        - if badge.claimcode && badge.claimcode != ""
          .claimcode= render :partial => 'claim_code'

#admin
  = link_to 'Add new badge (Admin Only)', new_badge_path
  = link_to 'View All Submissions (Admin Only)', submissions_path
  
